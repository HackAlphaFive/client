import { config, handleResponse } from "../../utils/api/api";
// import { TResponseGetTasks } from "../../utils/api/types";

// export function getTaskByIdIPR(id: number, signal?: AbortSignal) => {
//   return fetch(`${config.baseUrl}/iprs/${id}/tasks/`, {
//     signal: signal,
//     method: 'GET',
//     headers: {
//       ...config.headers,
//       authorization: localStorage.getItem('accessToken'),
//     }
//   }).then(handleResponse<TResponseGetTasks>)
// }
